(function(angular) {
	'use strict';
	define([ 'app' ], function(app) {
		app.register.controller('BuildingController', [ '$rootScope', '$timeout', '$interval', function($scope, $timeout, $interval) {
				
		}]);
	});
})(window.angular);

